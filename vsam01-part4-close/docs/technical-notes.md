# Technical notes
RRDS selected the third relative record with RRN 3. KSDS selected the record by key `00000003`. ESDS selected the record by RBA 160. IDCAMS PRINT first established that RBA 160 was the actual start address of `00000003CHARLIE`.

Validated evidence: RRDS load processed 5 records; the three targeted extraction jobs each processed one record; relevant IDCAMS executions completed with maximum condition code 0.
